{
  "bodyHash": "6415c270ef130cad",
  "content": "# MixinBooter\n### 让适用于模组的所有 Mixin 在 1.8 - 1.12.2 版本中轻松运行\n\n- 当前 Mixin 版本：[CleanMix 0.2.9 by CleanroomMC，基于 SpongePowered/Fabric Mixin (0.8.7) 的分叉版本](https://github.com/CleanroomMC/CleanMix)\n\n- 当前 MixinExtras 版本：[0.5.4](https://github.com/LlamaLad7/MixinExtras)\n\n### 伪更新日志：\n\n- 自 4.2 版本起，MixinBooter 的 API 已更改，***所有***使用 Mixin 的模组都建议依赖 MixinBooter，即使那些仅对原版/Forge/库类进行混入的模组也是如此。此举旨在避免与尝试实现模组化 Mixin 的其他模组（如 VanillaFix）发生版本不匹配导致崩溃的问题。感谢 [@embeddedt](https://github.com/embeddedt) 推荐并协助我引入此变更！\n\n- 自 5.0 版本起，[@LlamaLad7](https://github.com/LlamaLad7/MixinExtras) 开发的 [MixinExtras](https://github.com/LlamaLad7/MixinExtras) 已被阴影化（Shaded）。可供开发者使用。\n\n- 自 8.0 版本起，MixinBooter 将支持从 1.8 到 1.12.2 的所有版本。单一构建即可兼容所有这些版本！（待办事项：LiteLoader 支持？）\n\n- 自 8.4 版本起，MixinBooter 积极尝试与 [SpongeForge](https://github.com/SpongePowered/SpongeForge) 保持兼容。\n\n- 自 9.2 版本起，MixinBooter 恢复了用于 1.8.x 版本的旧版 MixinLoader 注解。\n\n- 自 10.0 版本起，MixinBooter 遵循 Mixin 0.8.7 标准。\n\n- 自 11.0 版本起，MixinBooter 基于 [CleanMix](https://github.com/CleanroomMC/CleanMix) 构建。这是为了与 [Cleanroom](https://cleanroommc.com/) 创建统一的后端。\n  - 您不再需要自行声明注解处理器的依赖。\n  - 新增配置文件 (`config/mixinbooter.cfg`)，用于黑名单 Mixin 配置和切换调试选项。\n  - 专用的 `logs/mixinbooter.log` Mixin 日志。具备追踪类加载功能，以实现精确调试。\n  - 允许传统的 `MixinConfig` + `MixinConnector` 清单属性条目完全融入生态系统。\n  - 针对 Mixin 所有者的模组发现功能，以及更完善的 `isModLoaded` 检查。\n  - 抑制 Forge 的 *损坏的 zip 文件* 警告。\n  - 弃用 Mixin 的“阶段”（Phases）机制。\n\n### 面向开发者 ~ 入门指南：\n\n1. 添加 CleanroomMC 的仓库并依赖 MixinBooter 的 Maven 条目：\n```groovy\nrepositories {\n    maven {\n        url 'https://maven.cleanroommc.com'\n    }\n}\n\ndependencies {\n    def mixin = 'zone.rong:mixinbooter:11.0'\n    \n    implementation (mixin) {\n        transitive = false\n    }\n    annotationProcessor mixin\n\n    // RetroFuturaGradle 用于生成 refmap：\n    modUtils.enableMixins(mixin)\n    // modUtils.enableMixins(mixin, 'mod_id.mixins.refmap.json') << 将 refmap 名称作为第 2 个参数添加（可选）\n}\n```\n\n2. 选择如何注册您的 Mixin 配置。MixinBooter 支持多种方法：\n\n（自 11.0 版本起，早期/晚期划分已不再存在，因此 IEarly/ILateMixinLoaders 已弃用）\n\n- **`MixinConfigs` 清单属性**：无需加载器类。在 JAR 包的清单中添加空格分隔的 Mixin 配置文件名列表。MixinBooter 会直接从 JAR 包清单中读取并注册它们。\n- **`MixinConnector` 清单属性**：以编程方式注册配置。指向一个实现了 `org.spongepowered.asm.mixin.connect.IMixinConnector` 的类，其 `connect()` 方法将在启动期间被调用，您可以在其中自行调用 `Mixins.addConfiguration(...)`。\n\n这两个清单属性均在您的 JAR 任务中设置：\n\n```groovy\njar {\n    manifest {\n        attributes(\n                'MixinConfigs': 'mixins.mymod.json',\n                'MixinConnector': 'com.example.mymod.MyMixinConnector'\n        )\n    }\n}\n```\n\n```java\npublic class MyMixinConnector implements IMixinConnector {\n    \n    @Override\n    public void connect() {\n        // 在注册前检查模组是否已加载？\n        // 可通过 zone.rong.mixinbooter.service.ModDiscoverer.isModPresent(String modId) 实现\n        Mixins.addConfiguration(\"mixins.mymod.json\");\n    }\n    \n}\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
