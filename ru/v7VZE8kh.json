{
  "bodyHash": "7857a0e91ae128d3",
  "content": "\n\n### [如果您喜欢本模组且经济允许，请考虑在 Ko-fi 上支持我，哪怕 1 美元我也非常感谢。](https://ko-fi.com/raguto)<a href=\"https://ko-fi.com/Y8Y11OOHHU\" target=\"_blank\" rel=\"noopener\"><img src=\"https://img.shields.io/badge/Ko--fi-Support%20Me-black?style=flat&logo=ko-fi\" alt=\"Ko-fi\">\n</a>\n\n---\n_**本人授权任何希望将本模组加入整合包的用户**_\n\n---\n# Wild Battle API\n\nWild Battle API 是专为 Cobblemon 打造的共享野生对战 AI 框架。\n\n对于玩家和整合包作者，它提供了可配置的更智能的野生对战行为，内置预设和管理员工具。对于开发者，它暴露了一个受支持的公共 API，用于注册自定义野生 `BattleAI` 实现，而无需直接替换 Cobblemon 的内部代码。\n\nWild Battle API 会在战斗开始时为野生对战实体安装一个委托 AI。在每次 AI 决策时：\n\n- 优先检查高优先级的提供者（Provider）。\n- 使用第一个返回非空 `BattleAI` 的提供者。\n- 返回 `null` 则会交由低优先级的提供者处理。\n- 如果没有提供者接管该上下文，则回退使用该实体的原始 Cobblemon AI。\n\n这使得多个模组能够安全协作，同时在无自定义提供者适用时保留可靠的重回退路径。\n\n## 包含功能\n\n- 用于野生对战 AI 扩展的共享 API\n- 基于优先级的提供者解析机制\n- 安全回退至实体的原始 Cobblemon AI\n- 内置可配置的智能野生 AI 行为\n- 从 `easy`（简单）到 `extreme`（极端）的难度预设\n- 管理员界面与调试命令\n- 支持 Minecraft `1.21.1` 的 Fabric 和 NeoForge 版本\n\n## 管理员命令\n\nWild Battle API 包含用于测试和配置的管理员/OP 命令。\n\n命令列表：\n- `/wildbattleapi ui`\n- `/wildbattleapi difficulty <easy|medium|hard|extreme>`\n- `/wildbattleapi debug on`\n- `/wildbattleapi debug off`\n- `/wildbattleapi debug status`\n\n别名：\n- `/wba ui`\n- `/wba difficulty <easy|medium|hard|extreme>`\n- `/wba debug on|off|status`\n\n执行这些命令需要权限等级 `2`.\n\n\n<details>\n<summary>开发者用法</summary>\n\n## 开发者用法\n\n### 支持的公共 API\n\n第三方集成仅可使用以下类：\n\n- `com.github.raguto.wildbattleapi.api.WildBattleAPI`\n- `com.github.raguto.wildbattleapi.api.WildAIProvider`\n- `com.github.raguto.wildbattleapi.api.WildAIContext`\n\n请勿依赖 `impl`、`mixin`、`config` 或 `network` 等内部包。\n\n### 运行时依赖\n\n请将 Wild Battle API 安装到您的依赖模组加载的任何位置。在常规整合包中，这意味着客户端和服务端都需要安装。\n\n模组 ID：\n- `wild_battle_api`\n\n同时请在您的模组元数据中将其声明为依赖项。\n\nFabric (`fabric.mod.json`)：\n\n```json\n{\n  \"depends\": {\n    \"wild_battle_api\": \"*\"\n  }\n}\n```\n\nNeoForge (`META-INF/neoforge.mods.toml`)：\n\n```toml\n[[dependencies.your_mod_id]]\nmodId = \"wild_battle_api\"\ntype = \"required\"\nversionRange = \"[1.0.0,)\"\nordering = \"AFTER\"\nside = \"BOTH\"\n```\n\n请将最低版本号调整为您模组所支持的首个 Wild Battle API 版本。\n\n### 编译时依赖\n\n如果您不使用已发布的 Maven 构件，请直接针对特定加载器的 jar 文件进行编译。\n\nFabric 示例：\n\n```gradle\ndependencies {\n    modCompileOnly files(\"libs/wild_battle_api-fabric-1.0.0.jar\")\n}\n```\n\nNeoForge 示例：\n\n```gradle\ndependencies {\n    compileOnly files(\"libs/wild_battle_api-neoforge-1.0.0.jar\")\n}\n```\n\n请根据您的环境使用对应加载器的 jar 文件。\n\n### 注册提供者\n\n请在您模组的公共初始化阶段注册一次您的提供者。\n\n```java\nimport com.github.raguto.wildbattleapi.api.WildBattleAPI;\nimport com.github.raguto.wildbattleapi.api.WildAIContext;\n\npublic final class MyWildAIRegistration {\n    public static void init() {\n        WildBattleAPI.registerProvider(150, context -> {\n            if (shouldUseMyAI(context)) {\n                return new MyCustomWildBattleAI();\n            }\n            return null;\n        });\n    }\n\n    private static boolean shouldUseMyAI(WildAIContext context) {\n        return context.getBattle() != null && context.getActor() != null;\n    }\n}\n```\n\n您也可以使用默认优先级进行注册：\n\n```java\nWildBattleAPI.registerProvider(context -> null);\n```\n\n### 优先级指南\n\n- `0`：通用捕获或默认提供者\n- `50`：标准覆盖\n- `100+`：高优先级或专属首领覆盖\n- `200+`：强制覆盖，请谨慎使用\n\n如果您的提供者仅处理特定情况，其他情况请返回 `null`。\n\n### 实现自定义 BattleAI\n\n您可以提供任意实现了 Cobblemon `BattleAI` 接口的类。\n\n```java\nimport com.cobblemon.mod.common.api.battles.model.PokemonBattle;\nimport com.cobblemon.mod.common.api.battles.model.ai.BattleAI;\nimport com.cobblemon.mod.common.battles.ActiveBattlePokemon;\nimport com.cobblemon.mod.common.battles.BattleSide;\nimport com.cobblemon.mod.common.battles.PassActionResponse;\nimport com.cobblemon.mod.common.battles.ShowdownActionResponse;\nimport com.cobblemon.mod.common.battles.ShowdownMoveset;\nimport com.cobblemon.mod.common.net.messages.client.battle.BattleHealthChangePacket;\nimport org.jetbrains.annotations.Nullable;\n\npublic final class MyCustomWildBattleAI implements BattleAI {\n    @Override\n    public ShowdownActionResponse choose(\n        ActiveBattlePokemon activeBattlePokemon,\n        PokemonBattle battle,\n        BattleSide side,\n        @Nullable ShowdownMoveset moveset,\n        boolean forceSwitch\n    ) {\n        if (forceSwitch || moveset == null) {\n            return PassActionResponse.INSTANCE;\n        }\n\n        // 替换为您自己的招式或切换逻辑。\n        return PassActionResponse.INSTANCE;\n    }\n\n    @Override\n    public void onHealthChange(BattleHealthChangePacket packet) {\n        // 可选的状态追踪。\n    }\n}\n```\n\n### WildAIContext\n\nWildAIContext 向提供者提供决定是否需要接管野生对战实体所需的信息。\n\n始终可用：\n- `getActor()`\n- `getPokemonList()`\n- `getBattle()`\n- `isForceSwitch()`\n\n根据当前决策可能为空：\n- `getActiveBattlePokemon()`\n- `getAiSide()`\n- `getMoveset()`\n\n在使用前请对可能为空的字段进行判空保护。\n\n### 集成注意事项\n\n- 保持提供者逻辑快速且可预测。\n- 当您的模组不打算接管当前上下文时，请返回 `null`。\n- 避免在提供者或 AI 逻辑中抛出异常。\n- 请在专用服务器和集成单人模式中均进行测试。\n- 如果多个模组注册了提供者，数值更高的优先级会优先被检查。\n\n</details>\n\n\n\n## 许可协议\n\nWild Battle API 的代码和二进制文件保留所有权利。\n\n第三方使用及分发权限由模组页面中的作者政策定义。",
  "contentType": "text/markdown",
  "type": "modrinth"
}
