{
  "bodyHash": "b0893149c9d6c44c",
  "content": "<h1>用于Create自动化的KubeJS Hooks</h1>\n<p>KJSCAuto允许KubeJS脚本修改Create中的关键自动化行为。</p>\n<p>此模组将若干Create操作暴露为脚本事件，使得包制作者无需编写Java模组即可拦截并修改这些行为。</p>\n<p>有意不包括配方修改功能，因为该功能已由KubeJS Create提供。</p>\n<p>当前支持的钩子包括：</p>\n<ul>\n<li>Deployer的物品/工具使用</li>\n<li>Basin配方操作</li>\n<li>钻头与锯子的方块破坏</li>\n<li>构装体拆解</li>\n</ul>\n<p>这些事件使脚本能够：</p>\n<ul>\n<li>调整Deployer工具的耐久度消耗</li>\n<li>修改Basin配方的输入和输出</li>\n<li>拦截或替换机器/构装体破坏方块时的掉落物</li>\n</ul>\n<p>选择这些钩子是为了解决KubeJS社区中经常讨论的几种常见自动化控制问题。</p>\n<p>如果您认为还有其他Create行为适合通过脚本来增强，欢迎在讨论论坛中提出建议。</p>\n<p>1.21.1引入了几个破坏性变更，因此以下示例在该版本中无法正常使用。请查阅 <a href=\"https://github.com/vomiter-scp-zh/KubeJS_Create_Automation/wiki/1.21-example\" target=\"_blank\" rel=\"nofollow\">wiki</a> 获取1.21.1的示例。</p>\n<p><strong>示例脚本：</strong>（全部放入服务器脚本。）</p>\n<h2>部署工具时不消耗耐久度</h2>\n<p>当在皮带/仓库下方放置钻石块时，Deployer获得一种特殊效果：<em>砂纸不再消耗耐久度，而打磨结果变为钻石。</em></p>\n<pre><code>KJSCAutoEvents.deployerUse(event =&gt; {\n    var {block, outputs, heldItem} = event\n    if(heldItem != \"create:sand_paper\") return //只对砂纸生效\n    if(!block.offset(0, -3, 0).blockState.is(Blocks.DIAMOND_BLOCK)) return; //仅在皮带/仓库(y = -2)下方有钻石块时生效\n    event.setDamage(0) //设置耐久度消耗为0\n    while(outputs.length &gt; 0) outputs.remove(0) //移除所有原始输出\n    outputs.add(0, Item.of(\"diamond\")) //添加钻石\n})</code></pre>\n<h2>混合羊毛和红染料而不消耗染料</h2>\n<p>此方法用于实现某些“催化剂”配方。</p>\n<p><strong>技术说明：</strong> <em>Create的Basin中，输出结果会保留在单独的输入/原料库存外，因此作为输出的物品不会被当作下一次操作的原料使用。</em></p>\n<pre><code>KJSCAutoEvents.basinOperation(event =&gt; {\n    var {basin, block, fluidOutputs, heatLevel, outputs, recipe} = event;\n    if(recipe.getId().toString() == \"minecraft:dye_red_wool\"){\n        event.addInput(\"red_dye\")\n    }\n})</code></pre>\n<h2>钻头的条件性精准采集</h2>\n<p>当满足特定条件时，钻头可获得类似精准采集的效果。如果在矿石下方放置紫水晶块，钻头将直接掉落矿石方块而非普通掉落物。</p>\n<pre><code>//适用于静止钻头。\nKJSCAutoEvents.blockDestroy(event =&gt; {\n    const { level, targetPos, targetBlock } = event\n    const below = level.getBlockState(targetPos.below())\n    if (!below.is(\"minecraft:amethyst_block\")) return\n    if (!(targetBlock.blockState.is(\"minecraft:diamond_ore\") || targetBlock.blockState.is(\"minecraft:emerald_ore\")))return\n    targetBlock.popItem(targetBlock.blockState.block.asItem())\n    level.destroyBlock(targetPos, false)\n    event.cancel()\n})\n\n//适用于构装体上的钻头。\nContraptionEvents.afterBlockDestroy(event =&gt; {\n    const { level, targetPos, targetBlock, drops } = event\n    const below = level.getBlockState(targetPos.below())\n    if (!below.is(\"minecraft:amethyst_block\")) return\n    if (!(targetBlock.blockState.is(\"minecraft:diamond_ore\") || targetBlock.blockState.is(\"minecraft:emerald_ore\")))return\n    while(drops.length &gt; 0) { drops.remove(0) }\n    drops.add(Item.of(targetBlock.blockState.block.asItem()))\n})</code></pre>\n<h2>构装体的保护地基</h2>\n<p>放置在灵魂土上的方块不能被构装体机器破坏。</p>\n<pre><code>ContraptionEvents.beforeBlockDestroy(event =&gt; {\n    const { level, targetPos } = event\n    const below = level.getBlockState(targetPos.below())\n\n    if (!below.is(\"minecraft:soul_soil\")) return\n\n    event.cancel() //阻止构装体破坏该方块\n})</code></pre>\n<h2>取消拆解</h2>\n<p>当拆解范围内存在海晶灯时，防止构装体拆解。</p>\n<pre><code>ContraptionEvents.beforeDisassemble(event =&gt; {\n    console.log(\"拆解\")\n    let shouldCancel = event.testTargetBlock(block =&gt; {\n        return block == \"minecraft:sea_lantern\"\n    })\n    if(shouldCancel) event.cancel()\n})</code></pre>\n<h2>支持</h2>\n<p>如您希望支持我的模组开发工作（包括维护、更新及兼容性改进），可通过Patreon进行支持：<a href=\"https://patreon.com/vomiter_scp\" target=\"_blank\" rel=\"nofollow\">https://patreon.com/vomiter_scp</a></p>",
  "contentType": "text/html",
  "type": "curseforge"
}
