{
  "bodyHash": "cVc6BGXWOhI=",
  "content": "# AdvancementJS\nНастройте больше достижений с помощью KubeJS\n\n## Бытвый пример (для последней версии `2.9.3`)\n\n### Скрипт запуска\n```js\n// Создаем пользовательский триггер\nAdvJSEvents.trigger(event => {\n    event.create(\"advjs:get_adv\")\n        // Сколько у вас было определено матчей, столько тестов вы должны добавить\n        // В этом примере мы определили 2 матча\n        .match(advancement => advancement.getId() == \"minecraft:story/smelt_iron\")\n        .match(playerName => playerName == \"Dev\")\n})\n```\n\n### Скрипт сервера\n```js\nAdvJSEvents.advancement(event => {\n    const { PREDICATE, TRIGGER } = event;\n\n    // Определяем триггер\n    const jump5times = TRIGGER.tick(triggerBuilder =>\n        triggerBuilder.addStat(Stats.JUMP, Stats.CUSTOM, { min: 5 }));\n    const bred_in_nether = TRIGGER.bredAnimals(triggerBuilder => {\n        triggerBuilder.setChildByPredicate(PREDICATE.entityFromJson({\n            stepping_on: {\n                dimension: \"the_nether\"\n            }\n        }))\n    });\n    // Пользовательский триггер AdvJS\n    const destroy_dirt = TRIGGER.blockDestroyed(triggerBuilder => triggerBuilder.setBlock(\"dirt\"));\n    // Ваш пользовательский триггер\n    const get_adv = TRIGGER.custom(\"advjs:get_adv\");\n\n    // Создаем корневое достижение\n    const root = event.create(\"advjs:hell\")\n        .display(displayBuilder => {\n            displayBuilder.setTitle(\"AdvancementJS\")\n            displayBuilder.setDescription(\"Быстрый пример\")\n            displayBuilder.setIcon(\"diamond\")\n        })\n        .criteria(criteriaBuilder => criteriaBuilder.add(\"dirt\", destroy_dirt))\n        .rewards(rewardsBuilder => {\n            rewardsBuilder.setExperience(100)\n            // Пользовательское вознаграждение AdvJS\n            rewardsBuilder.addEffect(\"absorption\", 200)\n        })\n        // Делаем его повторяемым\n        .repeatable();\n\n    // Добавляем дочернее достижение для корня\n    root.addChild(\"child1\", childBuilder => {\n        childBuilder\n            .display(displayBuilder => {\n                displayBuilder.setTitle(Text.red(\"Святое\"))\n                displayBuilder.setDescription(Text.red(\"Ад начинается\"))\n            })\n            .criteria(criteriaBuilder => {\n                // 'OR' означает, что для достижения этого достижения вам достаточно, чтобы один из двух нижеприведенных триггеров сработал\n                criteriaBuilder.setStrategy(RequirementsStrategy.OR)\n                criteriaBuilder.add(\"bred\", bred_in_nether)\n                criteriaBuilder.add(\"jump\", jump5times)\n                criteriaBuilder.add(\"get_adv\", get_adv)\n            })\n            .rewards(rewardsBuilder => {\n                rewardsBuilder.setRecipes(\"minecraft:lodestone\", \"minecraft:brewing_stand\")\n                rewardsBuilder.setExperience(100)\n            })\n    });\n\n    // Удаляем существующее достижение по фильтру AdvancementFilter; доступный фильтр описан в документации.\n    // Вы также можете удалить по ID: 'event.remove(\"minecraft:story/lava_bucket\");'\n    event.remove({\n        mod: \"minecraft\",\n        icon: \"minecraft:lava_bucket\",\n        frame: \"task\"\n    });\n\n    // Модифицируем существующее достижение\n    event.get(\"minecraft:story/smelt_iron\")\n        // Применяем смещение к отображению\n        .displayOffset(1, 1, true)\n        .modifyDisplay(displayBuilder => displayBuilder.setIcon(\"diamond_pickaxe\"))\n        .addChild(\"child2\", childBuilder => {\n            childBuilder\n                .display(displayBuilder => {\n                    displayBuilder.setIcon(\"recovery_compass\")\n                    displayBuilder.setTitle('Я вернусь!')\n                    displayBuilder.setDescription(Text.green(\"Удачи\"))\n                    // Вы также можете применять отображение в DisplayBuilder\n                    displayBuilder.offset(-1, 0)\n                })\n                // Триггер также можно создать из JSON\n                .criteria(criteriaBuilder => criteriaBuilder.add(\"go_back_to_home\", TRIGGER.fromJson({\n                    \"trigger\": \"minecraft:changed_dimension\",\n                    \"conditions\": {\n                        \"from\": \"minecraft:the_end\",\n                        \"to\": \"minecraft:overworld\"\n                    }\n                })))\n                // Проверяем, завершил ли родитель, иначе оно не будет выполнено\n                .requireParentDone()\n        });\n})\n\nAdvJSEvents.lock(event => {\n    event.result(\"stone_slab\", \"minecraft:story/smelt_iron\");\n    event.itemUse(\"spyglass\", \"minecraft:story/smelt_iron\");\n    event.blockInteract(\"chest\", \"minecraft:story/smelt_iron\");\n    event.entityInteract(\"villager\", \"minecraft:story/smelt_iron\");\n})\n\n// Совместимость с модом 'Better Advancements'\nAdvJSEvents.betterAdv(event => {\n    event.modify(\"advjs:hell/child1\").posX(0).posY(32).hideLines()\n})\n\nPlayerEvents.advancement(event => {\n    const player = event.getPlayer();\n    // Первый аргумент используется для сопоставления предиката игрока, два других — это определенные вами матчи\n    CustomTriggers.of(\"advjs:get_adv\").trigger(player, event.getAdvancement(), player.username)\n})\n```\n\n# Как перезагрузить\nПросто используйте команду `/reload`\n\n# Особенности\n- Команда `/advjs` для генерации встроенных примеров\n- Вы можете создать пользовательский триггер в событии запуска `AdvJSEvents.trigger`.\n- Пользовательский триггер AdvJS:\n  - blockDestroyed: срабатывает, когда игрок разрушает блок.\n  - playerTouch: срабатывает, когда игрок касается моба.\n  - bossEvent: срабатывает, когда игрок присоединяется к битве с боссом.\n  - increasedKillScore: срабатывает, когда игрок убивает моба.\n- Пользовательское вознаграждение AdvJS:\n  - addEffect: для применения эффекта.\n- Пользовательские методы AdvJS:\n  - displayOffset(offsetX: number, offsetY: number, modifyChildren?: boolean)\n    - применяет смещение к отображению достижения и его дочерним элементам\n  - requireParentDone()\n    - проверяет, завершен ли родитель, иначе оно не будет выполнено\n  - requireOthersDone(requires[]: ...ResourceLocation)\n    - проверяет, были ли выполнены указанные вами достижения\n  - requireAnyDone(requires[]: ...ResourceLocation)\n- Совместимость\n  - `Better Advancements`, новое серверное событие: `AdvJSEvents.betterAdv`",
  "contentType": "text/markdown",
  "type": "modrinth"
}
