{
  "bodyHash": "69eb818f20afadd9",
  "content": "# ![Cryonic Config](https://cdn.modrinth.com/data/oEhQIkOs/images/310961bc2931a273a4450b10a98a58d6744449d2.png)\n- 跨版本、易于移植的配置工具，依赖极少  \n- 专门针对易用性和可移植性进行优化\n- 名为“Cryonic”（低温休眠），因为它酷毙了\n\n# 功能说明\n- 配置文件将以“mod_id.json”的形式存储在 `{minecraft_dir}/config` 目录下\n- 所有配置文件将在早期初始化阶段通过 “cryonicconfig.json” 中的字典进行读取\n- 配置仅可通过直接编辑 JSON 文件进行修改，暂无图形界面（GUI）配置计划\n- 为了便于移植，同步变量会直接发送给玩家聊天栏，并被拦截处理\n- 使用哈希表（HashMaps）来查找从字符串到 ConfigStorage 以及从字符串到对象（变量）的映射\n\n# API\n## Groovy (build.gradle) 添加依赖\n添加 Maven 仓库：  \n```groovy\nrepositories {\n    maven { url \"https://api.modrinth.com/maven\" }\n}\n```\nModern/Architectury：  \n如果仅支持 Fabric 或 NeoForge，请将 `${project.name}` 替换为 `fabric`、`forge` 或 `neoforge`。\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:fabric-${project.name}:1.0.0+mc${rootProject.minecraft_version}\"\n}\n```\n\nb1.7.3:\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:babric:1.0.0+mcb1.7.3\"\n}\n```\n\n## 简单用法：\n\n```java\n// 你可以随时存储或使用配置，此方法可在任何地方调用\nConfigStorage config = CryonicConfig.getConfig(\"mod_id\");\n\n// 你可以存储整数、双精度浮点数、布尔值和字符串\n// 获取这些值的同时也会设置它们的默认值\n// 获取器格式：(变量名, 默认值)\n// 你无法复用变量名！它们会被覆盖。\nconfig.getInt(\"varName\", 3);\nconfig.getDouble(\"name\", 3.3);\nconfig.getBoolean(\"var\", true);\nconfig.getString(\"str\", \"Geronimo!\");\n\n// 该变量将在客户端和本地服务器上本地存在\n// 如果你希望连接服务器的玩家使用服务器端的配置\n// 你必须调用同步方法，使客户端使用服务端的值\nconfig.sync(\"varName\", playerEntity);\n\n// 欢迎使用这种简洁写法：\nCryonicConfig.getConfig(\"mod_id\").getInt(\"varName\", 3);\n\n// 你可以手动设置变量，而不是让获取方法自动生成\n// 这也适用于覆盖旧的值\nconfig.setInt(\"varName\", 3);\nconfig.setDouble(\"name\", 3.3);\nconfig.setBoolean(\"var\", true);\nconfig.setString(\"str\", \"Geronimo!\");\n```\n\n## 标记依赖\n在 `fabric.mod.json` 中：\n```json\n{\n  \"depends\": {\n    \"cryonicconfig\": \"*\"\n  }\n}\n```\n\n\n在 `mods.toml` / `neoforge.mods.toml` 中：\n```toml\n[[dependencies.cryonicconfig]]\nmodId = \"cryonicconfig\"\ntype = \"required\"\nversionRange = \"[0,)\"\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
