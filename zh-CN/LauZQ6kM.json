{
  "bodyHash": "5047783b0c1c2b1b",
  "content": "## 项目弃用\n\n简单来说，优化方法依然适用，但 Forge/NeoForge 不允许模组修改 EventBus。\n\n与 Minecraft 或 Forge 自身的类不同，EventBus 作为“库”加载而非游戏的一部分，因此无法通过 Mixin 或 Transformer 修改 EventBus。\n\n在 1.2.0 版本中，为了进一步提升性能，我使用了一些不严谨的 Hack（或者按你意愿称为“非标准方法”）来修改 `ASMEventHandler` 类，这在 1.18.2+ 版本中不幸已失效。这些版本现已归档，且可能不会再收到进一步更新。\n\n## FastEvent\n\n这是一个 Forge/Neoforge 优化模组，用于优化 Forge 中最基础的系统之一：事件系统。\n\n## 速度有多快\n\n为每个受支持的 Minecraft 版本创建测试用例并不困难，尤其是因为这些版本的事件系统各不相同。因此，我将提供一份我为 Cleanroom 项目提交的 PR 中的 JMH 基准报告，其中使用了与 FastEvent 相同的优化方法：\n\n### 注册 10,000 个事件监听器，发布事件 0 次：\n\n```\nBenchmark                               Mode  Cnt     Score     Error  Units\nBusPerformanceTest.register10000Legacy  avgt    5  1126.498 ± 284.633  ms/op\nBusPerformanceTest.register10000Modern  avgt    5  1058.961 ± 173.586  ms/op\n```\n\n快约 6.4%。\n\n### 注册 1,000 个事件监听器，发布事件 10,000 次：\n\n```\nBenchmark                                       Mode  Cnt      Score      Error  Units\nBusPerformanceTest.register1000test10000Legacy  avgt    5  4407.963 ± 4250.643  ms/op\nBusPerformanceTest.register1000test10000Modern  avgt    5  3550.578 ± 1991.352  ms/op\n```\n\n快约 24%。\n\n### 原始 PR\n\n[https://github.com/CleanroomMC/Cleanroom/pull/328#issuecomment-2801099504](https://github.com/CleanroomMC/Cleanroom/pull/328#issuecomment-2801099504)\n\n## 它是如何工作的\n\n（注意技术细节！）\n\n当开发者使用 `@EventBusSubscriber` 和/或 `@SubscribeEvent` 订阅事件处理程序时，EventBus 不能神奇地获取到事件处理程序，而只能获得一个 `Method` 对象。所以最直截了当的做法是直接使用这个对象：`method.invoke(...)`。这个调用最终会被 JVM 重定向回原始方法，使得事件处理程序在订阅后能够接收到事件。\n\n但是（`method.invoke(...)`）非常慢。为了加快它的速度，EventBus 会在运行时为找到的每个方法生成事件处理程序类，从而消除昂贵的基于反射的调用。\n\n但类生成引入了另一个 slowdown。为了更快，FastEvent 用 Lambda 构造替代了类生成，加速了事件处理程序的构建。另一个好处是 Lambda 是“隐藏的”，允许 JVM 执行更多优化。\n\n如果你懂一点 Java，下面的代码示例可能会对你更直观：\n\n```java\nclass Listen {\n    public void onEvent(Event event) {\n    }\n}\nListen lis = new Listen();\n\n// EventBus 会为每个事件处理程序生成一个新类\nclass IEventListener$Listen$onEvent implements IEventListener {\n    private Listen instance;\n    public IEventListener$Listen$onEvent(Listen instance) {\n        this.instance = instance;\n    }\n    @Override\n    public void invoke(Event event) {\n        instance.onEvent(event);\n    }\n}\nIEventListener handler = new IEventListener$Listen$onEvent(lis);\n\n// FastEvent 使用 Lambda 生成事件处理程序\nIEventListener handler = lis::onEvent;\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
