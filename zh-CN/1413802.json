{
  "bodyHash": "yZSO4CTcmx0=",
  "content": "<h2>一个足够用的价值</h2><p>本模组提供了一个类似于<a href=\"https://www.curseforge.com/minecraft/mc-mods/projecte\" rel=\"nofollow\">ProjectE</a>的物品价值系统，为物品价值提供了便捷且可高度扩展的参考方案。</p><p>需要前置模组KubeJS。</p><h3>基础价值</h3><p><img src=\"https://cdn.modrinth.com/data/cached_images/7a4b299020803ccda61358ba1f2d6b9cb7c06e1d.png\" alt=\"演示图\"></p><p>默认情况下，本模组为大多数基础材料提供了基础价值。</p><p>此外，您可以通过以下方法添加或修改物品的基础价值：</p><pre><code>OEVEvents.addItemValue(event =&gt; {\n    // 添加基础价值。\n    event.addBaseItemValue('minecraft:iron_nugget', 10);\n    // 通过匹配NBT添加额外价值，例如Tacz的枪支。\n    event.addExtraItemValue('{GunId:\"tacz:m700\"}', 500000);\n});\n</code></pre><p>如果物品未显示价值，则其价值为0。上述方法会添加+10的价值（例如，0变为10）。这些方法支持负数。</p><p>物品价值会向下取整（例如，0.5变为0）。</p><p>如果您需要与其他模组交互并随时读取价值，可以使用价值管理器：</p><pre><code>// 当玩家右键点击物品时，输出物品的价值。\nItemEvents.rightClicked(event =&gt; {\n    event.player.tell(OEV$ItemValueManager.getValue(event.item));\n});\n</code></pre><h3>处理价值</h3><p>如果某个物品没有基础价值，但存在产出它的配方，则该物品的价值将默认取所有配方中材料价值总和的最小值。</p><p>例如，红色染料可以通过红色郁金香（价值16）或甜菜根（价值64）合成，因此默认价值为16。</p><p>对于产出多个物品的配方，价值会自动除以数量。例如，一个金锭（默认价值648）可以合成9个金粒，每个金粒的价值为72。</p><p>原版配方默认使用简单求和法。</p><p>对于其他模组注册的新配方类型，您可以通过以下方式设置简单求和法：</p><pre><code>let $RecipeType = Java.loadClass(\"net.minecraft.world.item.crafting.RecipeType\");\nOEVEvents.addRecipeHandler(event =&gt; {\n    event.addSimpleRecipeHandler($RecipeType.BLASTING);\n});\n</code></pre><h3>自定义处理</h3><p>以下是一个简单模版，使合成物品的价值为材料价值总和的十倍：</p><pre><code>let $RecipeType = Java.loadClass(\"net.minecraft.world.item.crafting.RecipeType\");\nOEVEvents.addRecipeHandler(event =&gt; {\n    // 为处理价值添加自定义配方处理器，遍历所有配方。\n    event.addCustomRecipeHandler($RecipeType.CRAFTING, recipe =&gt; {\n        let value = 0;\n        // 遍历配方中的每种材料。\n        for (let ingredient of recipe.getIngredients()) {\n            // 获取单种材料的最小价值。例如，对于匹配标签如煤（价值128）或木炭（价值32）的材料，合成火把时取32。\n            let ingredientMinValue = event.getMinIngredientValue(ingredient);\n            // 处理材料为空或无对应价值的情况。直接复制此代码即可。\n            if (ingredientMinValue === event.getMaxInteger()) continue;\n            else if (ingredientMinValue === -1) return true;\n            value += ingredientMinValue;\n        }\n        value *= 10;\n        // 为单个配方设置最终价值。\n        return event.defaultSetRecipeValue(recipe, value);\n    });\n    // 辅助方法，输出包含所有已注册配方类型的数组。\n    console.log(event.getAllRecipeType());\n});\n</code></pre><h3>未来计划</h3><ul>\n<li>移植至其他版本。</li>\n</ul>",
  "contentType": "text/html",
  "type": "curseforge"
}
