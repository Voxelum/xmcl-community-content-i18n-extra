{
  "bodyHash": "6415c270ef130cad",
  "content": "# MixinBooter\n### 使任何适用于模组的 Mixin 都能轻松地在 1.8 - 1.12.2 版本中工作\n\n- 当前 Mixin 版本：[CleanMix 0.2.9 by CleanroomMC，基于 SpongePowered/Fabric Mixin (0.8.7) 的分支](https://github.com/CleanroomMC/CleanMix)\n\n- 当前 MixinExtra 版本：[0.5.4](https://github.com/LlamaLad7/MixinExtras)\n\n### 伪更新日志：\n\n- 从 4.2 版本开始，MixinBooter 的 API 已更改，**所有**使用 Mixin 的模组都建议依赖 MixinBooter，即使这些模组仅对原版/Forge/库类进行混入。为了避免因 Mixin 版本不匹配导致模组崩溃（例如尝试实现模组化 Mixin 时），请确保兼容性。（这里特别指出的就是 VanillaFix）。感谢 [@embeddedt](https://github.com/embeddedt) 推荐并协助我引入此变更！\n\n- 从 5.0 版本开始，[MixinExtras by @LlamaLad7](https://github.com/LlamaLad7/MixinExtras) 已被阴影打包（shaded）。可供开发者使用。\n\n- 从 8.0 版本开始，MixinBooter 将支持从 1.8 到 1.12.2 的所有版本。单个构建即可兼容所有这些版本！（待办：LiteLoader 支持？）\n\n- 从 8.4 版本开始，MixinBooter 积极尝试与 [SpongeForge](https://github.com/SpongePowered/SpongeForge) 兼容。\n\n- 从 9.2 版本开始，MixinBooter 重新引入了用于 1.8.x 版本的旧版 MixinLoader 注解。\n\n- 从 10.0 版本开始，MixinBooter 遵循 Mixin 0.8.7 标准。\n\n- 从 11.0 版本开始，MixinBooter 基于 [CleanMix](https://github.com/CleanroomMC/CleanMix) 构建。这是为了与 [Cleanroom](https://cleanroommc.com/) 创建统一的后端所做的努力。\n  - 您不再需要自行声明注解处理器的依赖。\n  - 还添加了配置文件 (`config/mixinbooter.cfg`)，用于黑名单 Mixin 配置和切换调试选项。\n  - 专用的 `logs/mixinbooter.log` Mixin 日志。具备追踪类加载以进行精确调试的能力。\n  - 允许传统的 `MixinConfig` + `MixinConnector` 清单属性条目完全融入生态系统。\n  - 针对 Mixin 所有者的模组发现功能，更好的 `isModLoaded` 检查。\n  - 抑制 Forge 的 *损坏的 zip* 警告。\n  - 弃用 Mixin “阶段”（phases）。\n\n### 开发者指南 ~ 入门：\n\n1. 添加 CleanroomMC 的仓库并依赖 MixinBooter 的 Maven 条目：\n```groovy\nrepositories {\n    maven {\n        url 'https://maven.cleanroommc.com'\n    }\n}\n\ndependencies {\n    def mixin = 'zone.rong:mixinbooter:11.0'\n    \n    implementation (mixin) {\n        transitive = false\n    }\n    annotationProcessor mixin\n\n    // 用于生成 refmap 的 RetroFuturaGradle：\n    modUtils.enableMixins(mixin)\n    // modUtils.enableMixins(mixin, 'mod_id.mixins.refmap.json') << 作为第 2 个参数添加 refmap 名称（可选）\n}\n```\n\n2. 选择如何注册您的 Mixin 配置。MixinBooter 支持多种方法：\n\n（从 11.0 版本开始，早期/晚期划分已不再存在，因此 IEarly/ILateMixinLoaders 已废弃）\n\n- **`MixinConfigs` 清单属性**：无需加载器类。在 JAR 的清单中添加空格分隔的 Mixin 配置名称列表。MixinBooter 直接从 JAR 清单中读取并注册它们。\n- **`MixinConnector` 清单属性**：以编程方式注册配置。指向实现 `org.spongepowered.asm.mixin.connect.IMixinConnector` 的类，其 `connect()` 方法将在启动期间被调用，您可以在其中自行调用 `Mixins.addConfiguration(...)`。\n\n这两种清单属性均在您的 jar 任务上设置：\n\n```groovy\njar {\n    manifest {\n        attributes(\n                'MixinConfigs': 'mixins.mymod.json',\n                'MixinConnector': 'com.example.mymod.MyMixinConnector'\n        )\n    }\n}\n```\n\n```java\npublic class MyMixinConnector implements IMixinConnector {\n    \n    @Override\n    public void connect() {\n        // 在注册前检查模组是否已加载？\n        // 可通过 zone.rong.mixinbooter.service.ModDiscoverer.isModPresent(String modId) 实现\n        Mixins.addConfiguration(\"mixins.mymod.json\");\n    }\n    \n}\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
