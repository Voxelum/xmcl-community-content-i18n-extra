{
  "bodyHash": "4e5c478795108369",
  "content": "## 足够的价值\n\n该模组提供了一个类似于 [ProjectE](https://www.curseforge.com/minecraft/mc-mods/projecte) 的Item价值系统，提供方便且高度可扩展的Item价值参考。\n\n需要 KubeJS 作为前置条件。\n\n### 基础价值\n\n![展示](https://cdn.modrinth.com/data/cached_images/7a4b299020803ccda61358ba1f2d6b9cb7c06e1d.png)\n\n默认情况下，该模组为大多数基本材料提供了基础价值。\n\n此外，您可以使用以下方法添加或修改物品的基础价值：\n\n```javascript\nOEVEvents.addItemValue(event => {\n    // 添加基础价值。\n    event.addBaseItemValue('minecraft:iron_nugget', 10);\n    // 通过匹配NBT增加额外价值，例如Tacz的枪械。\n    event.addExtraItemValue('{GunId:\"tacz:m700\"}', 500000);\n});\n```\n\n如果一件物品没有显示其价值，则其价值为0。以上方法将增加+10的价值（例如，0变为10）。这些方法支持负数。\n\n物品价值被向下取整（例如，0.5变为0）。\n\n如果您需要与其他模组交互并在任何时间读取价值，可以使用值管理器：\n\n```javascript\n// 当玩家右键点击物品时输出物品的价值。\nItemEvents.rightClicked(event => {\n    event.player.tell(OEV$ItemValueManager.getValue(event.item));\n});\n```\n\n### 加工价值\n\n如果一个物品没有基础价值，但有一个生产它的配方，那么该物品的价值将默认为所有配方成分价值的最低总和。\n\n例如，红色染料可以由红郁金香（价值16）或甜菜根（价值64）制成，所以默认价值将是16。\n\n对于生产多个物品的配方，价值会自动除以数量。例如，金锭（默认价值648）可以制作9个金 nugget，每个的价值为72。\n\n默认情况下，Vanilla配方使用简单的求和方法。\n\n对于其他模组注册的新配方类型，可以使用以下方法设置一个简单的求和方法：\n\n```javascript\nlet $RecipeType = Java.loadClass(\"net.minecraft.world.item.crafting.RecipeType\");\nOEVEvents.addRecipeHandler(event => {\n    event.addSimpleRecipeHandler($RecipeType.BLASTING);\n});\n```\n\n### 自定义加工\n\n这是一个简单模板，使制作的物品的价值是其原材料价值的十倍：\n\n```javascript\nlet $RecipeType = Java.loadClass(\"net.minecraft.world.item.crafting.RecipeType\");\nOEVEvents.addRecipeHandler(event => {\n    // 添加一个自定义配方处理器来处理价值，遍历所有配方。\n    event.addCustomRecipeHandler($RecipeType.CRAFTING, recipe => {\n        let value = 0;\n        // 遍历配方中的每种成分。\n        for (let ingredient of recipe.getIngredients()) {\n            // 获取单个成分的最小价值。例如，对于匹配的煤炭（价值128）或木炭（价值32）标签的成分，在合成火把时取32。\n            let ingredientMinValue = event.getMinIngredientValue(ingredient);\n            // 处理成分为空或没有相应价值的情况。直接复制此部分即可。\n            if (ingredientMinValue === event.getMaxInteger()) continue;\n            else if (ingredientMinValue === -1) return true;\n            value += ingredientMinValue;\n        }\n        value *= 10;\n        // 设置配方的最终价值。\n        return event.defaultSetRecipeValue(recipe, value);\n    });\n    // 帮助方法，输出包含所有已注册配方类型的数组。\n    console.log(event.getAllRecipeType());\n});\n```\n\n### 未来计划\n\n- 移植到其他版本。",
  "contentType": "text/markdown",
  "type": "modrinth"
}
