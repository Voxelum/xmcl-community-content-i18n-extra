{
  "bodyHash": "69eb818f20afadd9",
  "content": "\n\n# ![Cryonic 配置](https://cdn.modrinth.com/data/oEhQIkOs/images/310961bc2931a273a4450b10a98a58d6744449d2.png)\n- 跨版本、易于移植的配置工具，依赖极少  \n- 专为易用性与可移植性打造\n- 命名为 Cryonic 纯粹是因为它酷到爆\n\n# 功能说明\n- 配置文件将以 `mod_id.json` 的格式存储在 `{minecraft_dir}/config` 目录中\n- 所有配置文件将在游戏早期初始化阶段通过字典读取并保存至 `\"cryonicconfig.json\"`\n- 仅支持直接编辑 JSON 文件进行配置，暂无图形界面（GUI）配置计划\n- 为方便移植，同步变量会直接通过聊天栏发送给玩家并进行拦截处理\n- 使用 HashMap 将字符串映射到 ConfigStorage 实例与变量对象（Obj）\n\n# API\n## Groovy (build.gradle) 添加依赖\n添加 Maven 仓库：  \n```groovy\nrepositories {\n    maven { url \"https://api.modrinth.com/maven\" }\n}\n```\n现代/Architectury 适配：  \n若仅针对 Fabric 或 NeoForge，请将 `${project.name}` 替换为 `fabric`、`forge` 或 `neoforge`\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:fabric-${project.name}:1.0.0+mc${rootProject.minecraft_version}\"\n}\n```\n\nB1.7.3 版本：\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:babric:1.0.0+mcb1.7.3\"\n}\n```\n\n## 基础用法：\n\n```java\n// 你可以随时存储或使用，此方法可在任何地方调用\nConfigStorage config = CryonicConfig.getConfig(\"mod_id\");\n\n// 支持存储 int、double、boolean 和 String 类型\n// 调用 getter 时也会同时设置默认值\n// getter 格式为 (变量名, 默认值)\n// 请勿重复使用变量名！否则会被覆盖。\nconfig.getInt(\"varName\", 3);\nconfig.getDouble(\"name\", 3.3);\nconfig.getBoolean(\"var\", true);\nconfig.getString(\"str\", \"Geronimo!\")\n\n// 该变量会在客户端和本地服务器端本地存在\n// 如果你希望已连接服务器的玩家使用服务端配置\n// 必须调用此方法，使客户端采用服务端的值 \nconfig.sync(\"varName\", playerEntity);\n\n// 也可以直接使用这种链式写法：\nCryonicConfig.getConfig(\"mod_id\").getInt(\"varName\", 3);\n\n// 你可以手动设置变量，而不是依赖 getter 自动生成\n// 这也常用于覆盖旧的值\nconfig.setInt(\"varName\", 3);\nconfig.setDouble(\"name\", 3.3);\nconfig.setBoolean(\"var\", true);\nconfig.setString(\"str\", \"Geronimo!\");\n```\n\n## 声明依赖\n在 `fabric.mod.json` 中：\n```json\n{\n  \"depends\": {\n    \"cryonicconfig\": \"*\"\n  }\n}\n```\n\n\n在 `mods.toml`/`neoforge.mods.toml` 中：\n```toml\n[[dependencies.cryonicconfig]]\nmodId = \"cryonicconfig\"\ntype = \"required\"\nversionRange = \"[0,)\"\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
