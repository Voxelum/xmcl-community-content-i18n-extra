{
  "bodyHash": "rgXp3WN58bg=",
  "content": "<h3>概述</h3><p><strong>预加载技巧 (Preloading Tricks)</strong> 是一款仅面向开发者的模组，能够在模组加载器启动的极早期阶段实现高级操作。它提供了早期钩子、基于 Java Instrumentation 的类转换以及用于加载器扩展的 SPI 驱动回调。</p><p>支持环境：</p><ul>\n<li><strong>Fabric</strong> – 1.20.1、1.21+</li>\n<li><strong>Forge (LexForge)</strong> – 1.20.1</li>\n<li><strong>NeoForge</strong> – 1.20.6、1.21+</li>\n</ul><p>您可以在更多版本上测试，因为它依赖于加载器版本而非 Minecraft 版本。</p><hr><h3>功能特性</h3><ul>\n<li><p><strong>早期加载器入口点</strong>，通过 <code>PreloadingEntrypoint</code>（基于 SPI）实现</p>\n<ul>\n<li>使用 <code>PreloadingTricksCallbacks</code> 事件注册回调：<ul>\n<li><code>SETUP_LANGUAGE_ADAPTER</code> – 早期语言适配器设置阶段</li>\n<li><code>COLLECT_MOD_CANDIDATES</code> – 在发现前动态添加模组候选路径</li>\n<li><code>SETUP_MODS</code> – 修改模组列表（通过 <code>ModManager</code> API 添加/移除/查询）</li></ul></li></ul></li>\n<li><p><strong>基于 Instrumentation 的 <code>ClassTransform</code></strong>\n能够转换已加载的类，包括 Java 核心类和类加载器类。\n可在所有平台上通过 <code>MANIFEST.MF</code> 配置（见下文）。</p></li>\n<li><p><strong>Forge 变体检测</strong>（仅限类 Forge 环境）</p>\n<ul>\n<li>通过 <code>ForgeVariants</code> 在运行时检测特定的 Forge/NeoForge 版本</li></ul></li>\n</ul><hr><h3>使用方式 (Gradle)</h3><p>将 Maven 仓库和依赖添加到您的构建文件中：</p><p><strong>Kotlin DSL：</strong></p><pre><code>repositories {\n    maven(\"<a href=\"/linkout?remoteUrl=https%253a%252f%252fraw.githubusercontent.com%252fsettingdust%252fmaven%252fmain%252frepository%252f\" target=\"_blank\" rel=\"nofollow\">https://raw.githubusercontent.com/settingdust/maven/main/repository/</a>\")\n}\n\ndependencies {\n  // 推荐：使用基础构件（无分类器）。\n  // Gradle 变体匹配 / cloche 将自动解析正确的平台构件。\n  implementation(\"settingdust.preloading_tricks:PreloadingTricks:VERSION\")\n\n  // 可选：当您希望避免意外使用其他平台 API 时，可固定到特定分类器。\n  // implementation(\"settingdust.preloading_tricks:PreloadingTricks:VERSION:fabric\")\n  // implementation(\"settingdust.preloading_tricks:PreloadingTricks:VERSION:forge-service\")\n  // implementation(\"settingdust.preloading_tricks:PreloadingTricks:VERSION:neoforge-modlauncher\")\n  // implementation(\"settingdust.preloading_tricks:PreloadingTricks:VERSION:neoforge-fancy-mod-loader\")\n}</code></pre><p>将 <code>VERSION</code> 替换为最新版本（例如 <code>3.5.9</code>）。</p><p>如果您使用 <a href=\"https://github.com/terrarium-earth/cloche\" target=\"_blank\" rel=\"nofollow\">cloche</a>，则会根据加载器/Minecraft 属性自动选择对应变体。</p><hr><h3>ClassTransform 配置</h3><p>在所有平台上，将以下属性添加到您的 <code>MANIFEST.MF</code> 中：</p><pre><code>ClassTransformConfig: xxxx.classtransform.json</code></pre><p>多个配置可以用逗号分隔指定：</p><pre><code>ClassTransformConfig: first.classtransform.json,second.classtransform.json</code></pre><p>配置示例（<code>xxxx.classtransform.json</code>）：</p><pre><code>{\n  \"package\": \"settingdust.preloading_tricks.neoforge.transformer\",\n  \"transformers\": [\n    \"mod_setup_hook.FMLLoaderTransformer\"\n  ]\n}</code></pre><hr><h3>Forge 变体检测（仅限类 Forge 环境）</h3><p>允许模组仅在特定的类 Forge 加载器变体上加载。这使得在同一模组文件中通过 jar-in-jar 的方式同时包含 LexForge 和 NeoForge 的 JAR 包成为可能。</p><p><strong>配置：</strong></p><p>在 <code>MANIFEST.MF</code> 中指定您的模组目标变体：</p><pre><code>ForgeVariant: LexForge</code></pre><p>或</p><pre><code>ForgeVariant: NeoForge</code></pre><p>指定了 <code>ForgeVariant</code> 的模组仅在匹配的加载器变体上运行时才会加载，从而允许变体特定实现的安全共存。</p><hr><p><strong>为那些需要在 Minecraft 加载过程的最初阶段进行干预的开发者而设计。</strong></p>",
  "contentType": "text/html",
  "type": "curseforge"
}
