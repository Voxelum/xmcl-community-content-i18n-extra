{
  "bodyHash": "69eb818f20afadd9",
  "content": "\n\n# ![Cryonic Config](https://cdn.modrinth.com/data/oEhQIkOs/images/310961bc2931a273a4450b10a98a58d6744449d2.png)\n- 跨版本、易移植的設定工具，依賴極少  \n- 專門針對易用性與可移植性設計\n- 命名為 Cryonic（冷凍）是因為它酷到不行\n\n# 功能說明\n- 此模組會將 JSON 檔案儲存於 `{minecraft_dir}/config` 目錄下，檔名為 `mod_id.json`\n- 在初始化早期，所有設定檔都會透過 `cryonicconfig.json` 中的字典讀取\n- 設定僅能透過直接編輯 JSON 檔案進行調整，目前沒有開發圖形介面設定的計畫\n- 為了方便移植，同步的變數會直接透過聊天視窗發送給玩家，並由模組攔截處理\n- 使用雜湊映射（HashMap）來查找字串對應的 ConfigStorage 與物件（變數）\n\n# API\n## Groovy (build.gradle) 新增相依性\n新增 Maven：  \n```groovy\nrepositories {\n    maven { url \"https://api.modrinth.com/maven\" }\n}\n```\nModern/Architectury：  \n若僅使用 Fabric 或 NeoForge，請將 `${project.name}` 替換為 `fabric`、`forge` 或 `neoforge`。\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:fabric-${project.name}:1.0.0+mc${rootProject.minecraft_version}\"\n}\n```\n\nb1.7.3：\n```groovy\ndependencies {\n    modImplementation \"maven.modrinth:cryonicconfig:babric:1.0.0+mcb1.7.3\"\n}\n```\n\n## 簡單用法：\n\n```java\n// 你可以在任意位置隨時存取或儲存，此方法不受限制\nConfigStorage config = CryonicConfig.getConfig(\"mod_id\");\n\n// 你可以儲存整數、雙精度浮點數、布林值與字串\n// 透過取得它們，你也會同時設定其預設值\n// getter 格式 (變數名稱, 預設值)\n// 你無法重複使用變數名稱！它們會被覆寫。\nconfig.getInt(\"varName\", 3);\nconfig.getDouble(\"name\", 3.3);\nconfig.getBoolean(\"var\", true);\nconfig.getString(\"str\", \"Geronimo!\")\n\n// 該變數會同時存在於客戶端與伺服器的本機端\n// 若希望連線的玩家使用伺服器端的設定\n// 你必須呼叫此方法，使客戶端採用伺服器的值 \nconfig.sync(\"varName\", playerEntity);\n\n// 你也可以直接使用這種格式：\nCryonicConfig.getConfig(\"mod_id\").getInt(\"varName\", 3);\n\n// 你可以手動設定變數，而非依賴 getter 自動產生\n// 這也適用於覆寫舊值\nconfig.setInt(\"varName\", 3);\nconfig.setDouble(\"name\", 3.3);\nconfig.setBoolean(\"var\", true);\nconfig.setString(\"str\", \"Geronimo!\");\n```\n\n## 標記相依性\n在 `fabric.mod.json` 中：\n```json\n{\n  \"depends\": {\n    \"cryonicconfig\": \"*\"\n  }\n}\n```\n\n\n在 `mods.toml` 或 `neoforge.mods.toml` 中：\n```toml\n[[dependencies.cryonicconfig]]\nmodId = \"cryonicconfig\"\ntype = \"required\"\nversionRange = \"[0,)\"\n```",
  "contentType": "text/markdown",
  "type": "modrinth"
}
